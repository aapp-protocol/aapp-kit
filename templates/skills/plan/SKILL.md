---
name: plan
description: Canonical blueprint planning and lane routing (delegates to aapp-plan).
disable-model-invocation: false
argument-hint: "[idea, feature, or plan-id]"
---

# Plan (Native IDE Slash Command Shim)

When invoked via `/plan`, delegate directly to the canonical `/aapp-plan` skill procedure. Author durable blueprints in `.plans/current/` under Git-level Blast Radius protection instead of ephemeral IDE scratchpads.

*See `aapp-plan` (`.agents/skills/aapp-plan/SKILL.md`) for the complete procedure and execution rules.*
